---
- name: create project
  hosts: 192.168.122.5
  tasks:
    - name: create project
      tower_project:
        name: "{{ name }}"
        description: "{{ description }}"
        organization: "{{ organization }}"
        state: present
        validate_certs: false
